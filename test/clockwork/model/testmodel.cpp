#include <catch2/catch.hpp>

#include <unistd.h>

#include "clockwork/model/model_impl2.h"

using namespace clockwork::model;

TEST_CASE("Load model from disk", "[model]") {

    // std::string base_filename = "../resources/"
    // std::string so_filename = 

    // Model model = Model::loadFromDisk(so_filename, clockwork_filename, clockwork_weights_filename);

    REQUIRE(true);
}